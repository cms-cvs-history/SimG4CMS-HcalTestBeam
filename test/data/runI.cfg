process GEOM = {
	untracked PSet maxEvents = {untracked int32 input = 0}
	source = EmptySource { }

	#Geometry
        include "SimG4CMS/HcalTestBeam/test/data/TB2007GeometryXML.cfi"

        module m = GeometryProducer { 
            bool UseMagneticField = false
	    bool UseSensitiveDetectors = false
            PSet MagneticField = { double delta = 1. }
        }
        path p1 = { m }
}
